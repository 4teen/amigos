<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_profile"
            android:icon="@drawable/ic_perm_identity_black_24dp"
            android:tint="@color/button_grey"
            android:title="@string/my_profile" />
        <item
            android:id="@+id/nav_add_friends"
            android:icon="@drawable/ic_friends_add_black_24dp"
            android:tint="@color/button_grey"
            android:title="@string/add_friends" />
        <item
            android:id="@+id/nav_search_class"
            android:icon="@drawable/ic_search_black_24dp"
            android:tint="@color/button_grey"
            android:title="@string/search_class" />
    </group>

</menu>